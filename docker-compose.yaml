version: '3'

services:
  spotify-test:
    image: golang:1.21
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - GOCACHE=/app/.cache/data
      - GOMODCACHE=/app/.cache/mod
    env_file:
      - .secrets.env
    command: go run /app/cmd/spotify
